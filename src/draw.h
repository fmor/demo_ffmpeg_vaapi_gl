#ifndef INCLUDE_DRAW
#define INCLUDE_DRAW

#include <GL/eglew.h>
#include <cglm/cglm.h>

#include "data.h"

void draw_cube( Context* ctx );
void draw_fullscreen( Context* ctx );
void draw_quad( Context* ctx );

#endif
